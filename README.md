Setup:

python -m venv .venv

source .venv/Scripts/activate

pip install -r requirements.txt

Setting up your user: 

create a file ".env"

DB_HOST=host ip 
DB_PORT=3306
DB_USER=user
DB_PASSWORD=password
DB_NAME=MatchDB
